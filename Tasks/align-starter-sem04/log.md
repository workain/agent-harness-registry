# log.md — ход работы

Пишется по ходу. Дата/время — UTC. Всё, что названо проверенным, проверено командой, вывод
которой приведён либо в соответствующем коммите, либо в `Tasks/seminar-findings.md`.

## 24.09.2026, ~04:40 — чтение первоисточников

Прочитан шаблон целиком (16 файлов на входе). Прочитан
`rework/section-1-fayl-instrukciy-part3.md` (377 строк) и слайды s04, s07, s11, s14, s19, s20,
s21, s27, s34, s35, s39, s43, s52, s55, s56, s57. Прочитан отчёт предыдущей ревью-сессии.

Что подтвердилось дословно: правило трёх адресов и таблица порогов включения (`part3`, s27);
хук отнесён к Семинару 5 (s19, s56); ADR — «по сигналу» (s55); день-0-список s07 из четырёх
позиций.

## ~04:44 — воспроизведение состояния демо-репозитория

```
npm ci                -> found 0 vulnerabilities
npm run build         -> ✓ built in 139ms
npx playwright test   -> 4 passed (5.4s)
самотест ворот        -> RESULT: PASS — 9/9 checks + 5 LIMIT
```

Существенное, чего в отчёте предыдущей ревью не было: **вся история репозитория создана
23.09.2026 за два с половиной часа** (`git log --date=iso-strict`), с 15:16:00 по 17:48:04.
Значит «день 0 = 20.09» и «запись от 26.09» со слайдов не подтверждаются ничем, а 26.09 — это
вообще будущее.

## ~04:45 — то, что предыдущая ревью проверить не смогла

Сеть в окружении флаки: `curl` к `code.claude.com` падает 2–4 раза, потом отвечает 200.
Предыдущая ревью на этом остановилась и пометила цитату про `/compact` как непроверенную. С
третьей попытки страница скачалась (733 КБ), обе цитаты сверены дословно — верны.

Тогда же проверено то, чего никто не проверял: **утверждение шаблона про `cp -R` без `-P`
ложно.** На GNU coreutils 9.4 симлинк сохраняется; ломает только `-L`. Верное и проверяемое
утверждение оказалось другим и сильнее: POSIX.1-2024 оставляет дефолт неопределённым.

## ~04:50 — правка шаблона

Issue #74 с пятью находками, ветка, коммит `d77532f`, PR #75. Классификация: 4×B (правило
впрок; ложное `cp`; загрузочная последовательность блокируется своими же воротами; слово
«ворота» в двух смыслах) + 1×C (разметка дня 0). Ничего не выпилено по причине «семинар ставит
это позже».

`.claude/rules/tests.md` удалён, механизм уехал в `doc/deferred.md` (переименован из
`doc/claude-md-sections.md`) готовым к вставке блоком с порогом включения.

## ~05:00 — независимый ROAST №1: BLOCK

Четыре блокера, два из них — дефекты, внесённые моим же PR. Воспроизвела оба сама:

1. «хук отклонит ваш первый коммит» — **ложно для человека в терминале.** Это хук Claude Code,
   `git init` git-хука не ставит, коммит на `main` из оболочки проходит с кодом 0. Я приняла
   за доказательство вызов команды хука напрямую.
2. Моя же инструкция `git switch -c` до первого коммита **уничтожала `main`**: на неродившемся
   HEAD она переименовывает ветку. `git rev-parse --verify main` → fatal, `.git/refs/heads/`
   пуст, и хук охраняет ветку, которой нет.

Плюс: слово «gate» в двух общих файлах (моё «шаблон их больше не путает» — ложь); метки
«по поводу» против день-0-указателей; правка `$comment` сделала базовый шаблон менее точным.
Исправлено коммитом `ecc9caa`. Тогда же по находке N3 убран `doc/adr/0001` — ADR со статусом
`Accepted` от имени пользователя, который его не принимал.

## ~05:05 — демо-репозиторий, первый заход

Собрала дугу веткой от `25bd3f0` (до хука). Оператор уточнил форму: **поверх `main`, с честным
коммитом-откатом.** Пересобрала: `1019546` (откат + итог кейса 1.2), далее кейсы.

Тогда же оператор поправил мою собственную оценку: кейс 1.3 надо вести как ДВЕ правки. s27
строка 39 и речь строка 56 утверждают «конвенция уже воплощена в ТРЁХ существующих тестах» —
это утверждение о существующем репозитории, где тест был один. Класс A, а не C. Разбила на
`a1bd73f` (класс A) и `ffbb258` (класс C).

Живая сборка вскрыла то, чего чтением не видно: **функции `submitForm()` в репозитории не
существовало вообще** — форма уходила нативным браузерным POST, без `fetch`, без таймаута, без
повтора. То есть посылка кейса 1.3 не могла быть правдой ни про одну строку этого кода.

## ~05:15 — мутационная проверка вместо зелёного

Первая мутация (`attempt <= 2` → `3`) тесты НЕ уронила. Разбор показал, что число попыток
кодировалось в двух местах и эффективным было другое. Вынесла в одну константу `MAX_ATTEMPTS`,
повторила: мутация роняет тест «is not retried a third time». То есть комментарий к функции
теперь чем-то удерживается, а не только прочитан.

## ~05:25 — кейсы 2.1–2.3, README, ветка Семинара 5

`94c5378` (запись в `DECISIONS.md`, реальная дата), `3b4b289` (`doc/adr/`, три настоящих ADR),
`14fa124` (`.tasks/` + блок в `CLAUDE.md`), `76106ac` (README с разделом «чем расходится со
слайдами»). Ветка `seminar-5-hook`: хук возвращён, `cmp` против реестра @ `3341756` —
IDENTICAL, самотест PASS 9/9.

Проверено из свежего клона: `main` — предок дуги (мерж будет fast-forward), 8 passed, симлинк
цел, вложенных файлов инструкций нет. `main` не тронут.

## ~05:35 — независимый ROAST №2: BLOCK. Все четыре блокера — из моего же раунда правок

1. Легенда «сверх дня 0 — УЖЕ включено» против строки `doc/adr/`, где по этому же коммиту
   записей нет. Шестнадцать строк друг от друга.
2. Deep-dive утверждал, что `.git/refs/heads/` «holds only the new name», и помечал это словом
   «Reproduced». Пуст: `ls -A | wc -l` → 0, `for-each-ref | wc -l` → 0. При этом README
   шаблона говорит верно — два моих же артефакта противоречили друг другу.
3. `integration:` в yaml сломан моим патчем: повисший глагол «Run», команда коммита не дана
   вовсе, и `rm -rf .git`, про который коммит-сообщение утверждало «dropped». Уцелело потому,
   что `integration:` в `GUIDE.md` не рендерится — ни один гейт его не видит.
4. Новая формулировка `$comment` ложна для starter: `grep -c '^## Repository etiquette'
   CLAUDE.md` → 0. Заменила одну ложную ссылку на другую.

Все четыре воспроизведены своими командами, исправлены коммитом `b449b37` вместе с девятью
не-блокирующими. Заказан ROAST №3.

## ~05:45 — `Tasks/seminar-findings.md`

Собран последним, как просил оператор: 8 находок по слайдам, 3 по `captures/`, 5 вопросов,
8+1 по Семинару 5. Статусы `[ПРОВЕРЕНО]`/`[ПЕРЕНЕСЕНО]`/`[НЕ ПРОВЕРЕНО]` разделены.

Две поправки к отчёту предыдущей ревью, обе в сторону понижения статуса:

- 18 файлов в `captures/` не «удалены расколом `e0d72dd`», а **перемещены** в
  `sem-05/assets/captures/` — все 18 на месте. Дефект только в устаревшей описи.
- Ссылка на `07-skills.md §4.1` неверна в одном месте (`part2:243`), а не в четырёх:
  `order.md:293` ссылается на §4.1 за разбор 13 скиллов курса — это действительно §4.1;
  `order.md:319` ссылается на §4.3 — тоже верно.

## ~05:55 — самопроверка того, что я взяла со слов ревьюера

Прежде чем ждать вердикт раунда 3, проверила два утверждения о документации Claude Code, которые
внесла в шаблон по отчёту ROAST, не открыв страницу. Открыла (сеть потребовала 3 и 2 попыток).
Оба подтвердились, но оба пришлось сузить: «ронял `Read` на КАЖДОМ файле» против «for every file
**the rule was evaluated against**», и цитата про интерактивную сессию была оборвана посреди
предложения при пометке «quoted verbatim».

И рядом нашлось третье, которого не заметили ни раунд 1, ни раунд 2: общий `$comment` обещал, что
хук «is live automatically in every fresh clone/worktree — no manual install step». По
`hooks.md` § Workspace trust это неверно для интерактивной сессии. Проверила, откуда это взялось:

```
$ git show origin/main:templates/base-project-template/with-git/.claude/settings.json | …
live automatically: 1
trust: 0
```

То есть пере-обобщение уже смержено в публичный реестр, а не появилось в этом PR. Починено в
обоих файлах одним изменением, чтобы `cmp` остался IDENTICAL; `md5sum` обоих на ветке —
`9e7fd26f9b8dc938d421efa26ecc2582`.

## ~06:00 — независимый ROAST №3: BLOCK, три блокера, все новее отчёта раунда 2

Все три воспроизведены своими командами. Самый неприятный — второй: `doc/adr/README.md`
предписывал удалить «строку-указатель», а такой строки нет: `doc/adr/` — вторая половина строки,
первая половина указывает на `DECISIONS.md`. Буквальное выполнение сносило живой указатель на
живой файл дня 0. И до моего раунда 2 `CLAUDE.md` и этот файл ошибались одинаково; мой раунд
превратил единую ошибку в самопротиворечие.

Исправлено `d813941` вместе с одиннадцатью не-блокирующими. После правки инструкцию по удалению
`doc/adr/` прогнала целиком на чистой копии: указатель на `DECISIONS.md` цел, висячих указателей
нет.

## ~06:05 — раунд 4 заказан, head заморожен

`039d469`. Раунд 3 отдельно отметил, что во время его работы head двигался пять раз и это
мешало — поэтому на раунд 4 ничего не пушу.

## ~06:15 — независимый ROAST демо-репозитория (заказан оператором): BLOCK, 2 блокера

Мерж PR #1 не мой, значит и ревью не моё. Обе находки воспроизвела своими командами.

1. README дуги утверждал «ни одна дата в git не переставлена и ни один коммит не переписан».
   Про `main` верно. Про дугу — нет: пять коммитов имеют одну и ту же секунду коммиттера
   (`05:30:57`) при авторских датах, разъезжающихся на одиннадцать минут и идущих по цепочке
   вразнобой. Это подпись моего же rebase'а при переделке «ветка от 25bd3f0» → «поверх main».
2. Файл задачи приводил `7d0fd16`, `90cbaf7`, `d85f26c` как свидетельство сделанных кейсов.
   Для читателя их не существует.

Второе — важнее первого, потому что **проверка, к которой я бы потянулась, прошла бы**:
`git cat-file -t 7d0fd16` в моём клоне отвечает `commit` (объекты живы как недостижимые), а в
свежем клоне — `fatal: Not a valid object name`. Верная проверка — достижимость с
опубликованной ветки; она заведена в сам файл задачи вместе с причиной, почему `cat-file` не
годится.

Файл задачи переписан целиком: кроме трёх фантомных SHA в нём был неразделённый кейс 1.3, два
шага помечены незакрытыми, хотя сделаны, и блок «что уже проверено» ссылался на тот же фантом.
Все одиннадцать SHA проверены механически. Исправлено `9dadb4b`, плюс `2643dfd` — таблица
коммитов в README теперь говорит, что покрывает кейсы, а не всю дугу. `seminar-5-hook`
пересобрана от новой вершины, самотест снова PASS 9/9.

## ~06:25 и ~06:50 — ROAST №4 и №5 по шаблону: BLOCK и BLOCK

Раунд 4 (`a590272`): четыре блокера, все внесены правками раунда 3, плюс второе ложное
утверждение в моём коммит-сообщении («zero occurrences of "live automatically"» — осталось одно,
в yaml). Существеннее прочего: `DECISIONS.md` содержал ВТОРОЙ указатель на `doc/adr/`, а четыре
артефакта утверждали, что указатель один; моё коммит-сообщение объявляло обратное как результат
проверки, потому что я грепнула `CLAUDE.md`, а не дерево.

Раунд 5 (`7f0d879`): снова четыре, снова все из предыдущей правки. И главное — **блокер 2
раунда 4 сам был неверен, а я его принял.** Я проверила цитату, на которую он ссылался
(оговорка про родительский каталог существует), и не проверила вывод: связанная страница
`permissions.md` (строки 642, 643, 665) говорит, что доверие родителя не распространяется на
вложенный репозиторий, а `git init -b main` в загрузке ровно его и создаёт. То есть моя
исходная фраза была верна, и я заменила правду на ложь, потому что ревьюер был убедителен.
Проверила сама, с первой попытки, вернула верный вывод с обоими источниками.

Структурный вывод, который я наконец сделала: три раунда подряд ломалась одна и та же правка —
инструкция «удалите указатели», написанная как СПИСОК МЕСТ. Список устаревал быстрее, чем его
чинили. Инструкция перестала быть списком: одна команда по дереву плюс разбор того, что
найдётся, на три вида. После этого оба удаления прогнаны от начала до конца, и вывод совпал с
обещанным.

Шестой раунд я не заказывала — вынесла оператору как решение, с доводами в обе стороны.

## Что осталось и за кем

- Вердикт ROAST №3 — за субагентом. Дальше: если BLOCK, воспроизводить каждую находку своей
  командой до правки.
- Мерж PR #75 — за оператором/менеджером, через `scripts/safe-merge.sh`. Свои PR не мержу.
- Мерж `seminar-4-arc` в `main` демо-репозитория — за оператором.
- Правка ветки семинара по `Tasks/seminar-findings.md` — за её автором.
