# Log — agent-harness-registry#52: MCP note + growth-ladder row

## Context

Order 2 of the seminar-driven gap audit of `templates/base-project-template`. Full spec:
https://github.com/workain/agent-harness-registry/issues/52

Sources read in full before starting (public, pinned commit `7f224dc05`):
- `10-template-work-order.md` — intro sections + § "Наряд 2" (the task's own spec, verbatim).
- `05-mcp.md` — incidents table, token-cost table, lethal-trifecta framing.

## Environment note — shared checkout collision (not part of the task, but affected how work was done)

This session's assigned folder (`/home/harness/harness-projects/1/ahr-sem04`) turned out to be a
**shared canonical checkout** used concurrently by sibling sessions working orders 1/3/4/5/6/8 —
one physical working tree, one `HEAD`, being switched between branches by multiple sessions at
once (confirmed live: `HEAD` moved under me between two `git status` calls, and a sibling working
order 1 independently flagged the same thing and moved to an isolated worktree). Per the task's
own binding rule ("never `git checkout` in a shared canonical checkout"), I did not touch that
directory. Instead: `git worktree add -b issue-52-mcp-notes ../ahr-sem04-wt52 origin/main` — a
fresh, isolated worktree off `origin/main`, matching the pattern already used for order 8
(`ahr-sem04-wt58`). All work below happened in `ahr-sem04-wt52`.

`GH_TOKEN` is not set in this environment. `workain/agent-harness-registry` is a **public** repo,
so unauthenticated `GET` against the GitHub REST API works fine for reading issues/PRs — used that
to fetch issue #52's body and check issue #51 / open-PR state. Write actions (opening the PR) will
need a token; noting this as a blocker to resolve before that step.

## What was done

1. **`common/.claude/mcp-notes.md`** — new file, 5 points, matching the issue's own "Что сделать"
   list almost verbatim (it already specified the 5 points): don't reach for MCP before checking
   whether Bash + an existing CLI (`gh`/`psql`/`aws`) suffices; `.mcp.json` at project scope,
   reviewed as code; keep the connected set minimal (context tokens + attack surface per server);
   lethal-trifecta check before connecting (private data + untrusted content + outbound channel →
   split across sessions instead); HTTP transport → OAuth only, never a pasted long-lived token.
   Opens with a one-line pointer to this registry's own `deep-dives/components/access-mcp/`
   catalog instead of re-explaining MCP — this is the "link outward, don't write a tutorial"
   guard the work order itself calls for (§ "Риск переусложнения").
2. **Growth ladder** — exactly one row added to
   `deep-dives/components/instructions-rules/base-project-template/design-and-usage.md`:
   axis "Access to external systems", trigger "An ordinary command is no longer enough", action
   "Read `.claude/mcp-notes.md`, then add a scoped `.mcp.json` entry — not a speculative
   connection." Same terseness as the 5 existing rows.
3. **`fragments/claude-core-bottom.md`** — one line added to § "Where things live", directly after
   the existing Subagents line, same shape: `- MCP (connecting to an external system):
   \`.claude/mcp-notes.md\`.`
4. **Both READMEs, hand-edited** (not generated by `render_templates.py`): one row added to the
   "What's here" table in `with-git/README.md` and `without-git/README.md`, each describing
   `.claude/mcp-notes.md` and its safe-to-delete answer ("read it before your first `.mcp.json`
   entry, not after").

## Honesty / provenance note

The work order's own token-cost comparison (1 365 tokens via CLI vs. 44 026 via MCP for the same
action) is explicitly marked in `05-mcp.md`'s own sources table as **blog-sourced, not
independently re-checked** (`myclaw.ai` blog post). `mcp-notes.md` deliberately does **not**
restate that specific figure at all — the note is 5 points, not a citations page, and repeating a
number without its caveat inline would launder a marked figure into an unmarked one. The general
claim it does carry ("every server is permanent context tokens... every request") is supported by
multiple independently-sourced measurements in `05-mcp.md` §3 (Anthropic's own ~55k/5-server
figure, the `zhang-liz/mcp-token-benchmark` 26 224-token figure, the DEV Community 11-server audit)
that don't carry that same caveat, so no `[unverified]` tag was needed there.

## Verification

```
$ python3 templates/base-project-template/render_templates.py
wrote with-git/CLAUDE.md
wrote without-git/CLAUDE.md
... (24 files total)
$ python3 templates/base-project-template/render_templates.py --check
render_templates.py --check: PASS — both variants match their source fragments/common files.
```

`git status --short` after render — exactly the expected file set changed, nothing else:
```
 M deep-dives/components/instructions-rules/base-project-template/design-and-usage.md
 M templates/base-project-template/fragments/claude-core-bottom.md
 M templates/base-project-template/with-git/CLAUDE.md
 M templates/base-project-template/with-git/README.md
 M templates/base-project-template/without-git/CLAUDE.md
 M templates/base-project-template/without-git/README.md
?? templates/base-project-template/common/.claude/mcp-notes.md
?? templates/base-project-template/with-git/.claude/mcp-notes.md
?? templates/base-project-template/without-git/.claude/mcp-notes.md
```

`diff` confirms `mcp-notes.md` is byte-identical across `common/`, `with-git/`, `without-git/`.

## Acceptance criteria (issue #52) — status

- [x] `mcp-notes.md` exists, ≤5 points (exactly 5), identical in both variants after render.
- [x] Growth ladder gains exactly one row, not a paragraph.
- [x] `claude-core-bottom.md` pointer is one line.
- [x] Both variants' README "What's here" tables gain one row.
- [x] `render_templates.py --check` → PASS.

## Still open

- Confirm issue #51 hasn't merged before opening the PR (was open, no PR yet, as of last check) —
  re-check + rebase immediately before opening the PR per the task's sequencing note.
- Independent ROAST — separate reviewer, not this session.
- PR opening needs a way to write to the GitHub API (no `GH_TOKEN` in this environment, `gh` not
  installed) — resolving before that step.
