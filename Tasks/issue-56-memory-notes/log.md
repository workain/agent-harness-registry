# Log — issue #56: memory note + growth-ladder row

## 2026-09-20 — shared-checkout collision, moved to isolated worktree

Started work in `/home/harness/harness-projects/1/ahr-sem04` (the session's own configured
folder) and ran `git checkout -b issue-56-memory-notes` there at ~12:37:45. That checkout turned
out to be **shared across all of orders 1–7** running in this folder this session (a
folder-setup issue, not anything any individual order session did wrong) — not per-session
worktrees. Within the next ~25 seconds, other order sessions (`issue-51-security-boundaries`,
`issue-53-skill-example`, others) checked out their own branches over mine in the same physical
tree, and by the time I ran `git branch --show-current` it reported `issue-53-skill-example`,
confirming the collision.

Two sibling sessions (order 1 / "ahr51-security-paragraph", and the folder-owning session
"for-the-seminar") flagged this mid-turn and instructed every order session to move into its own
`git worktree`. I had **not yet written or committed any file** at the point of the collision
(only read files and fetched sources), so nothing was lost — confirmed via
`git log --oneline -1` on the new worktree showing `7b7c678` (== `origin/main`, no local
commits) and `git status --porcelain` clean.

**Action taken:** created an isolated worktree —

```
git -C /home/harness/harness-projects/1/ahr-sem04 worktree add \
    /home/harness/harness-projects/1/ahr-sem04-wt56 issue-56-memory-notes
```

— and moved all further work there. Any earlier "render_templates.py --check" observation
would have been void per the same warning; no such run had actually happened yet, so there is
nothing to discard. From this point on, every command in this log ran with cwd
`/home/harness/harness-projects/1/ahr-sem04-wt56` (stated explicitly per command since this
session's Bash tool does not reliably persist `cd` across calls).

## Sources fetched (provenance)

- `10-template-work-order.md` @ commit `7f224dc058c171e05f81bb8d8def865e69ace5c2` — intro
  sections + § "Work order 6" (the spec verbatim, including the "don't become a second instruction
  file / don't overlap LESSONS.md/DECISIONS.md" bound, and the exact 5-point structure asked
  for).
- `02-memory.md` @ same commit — §4.2 (memory poisoning: SpAIware, EchoLeak/CVE-2025-32711),
  §4.3 (context bloating, ~200-line/25KB threshold), §5 table (CrewAI Memory vs LongMemEval:
  46.0% memory-enabled vs 57.6% memory-disabled, same benchmark, CrewAI issue #5800,
  "an independent LongMemEval run").
- Existing repo files read directly: `fragments/claude-core-bottom.md`,
  `deep-dives/components/instructions-rules/base-project-template/design-and-usage.md`
  (growth-ladder table), `common/LESSONS.md`, `common/DECISIONS.md`,
  `common/.claude/environment/README.md` + `_example.md`, `common/.claude/skills/README.md`,
  both variant `README.md`s.

## Plan

1. `common/.claude/memory-notes.md` — new file, ≤5 bullets, in `common/.claude/` (not
   `environment/` — discipline for an already-built-in runtime feature, not substrate
   knowledge).
2. One-line pointer added to the "Persistent memory" bullet in
   `fragments/claude-core-bottom.md`.
3. Exactly one new row in the growth-ladder table in `design-and-usage.md`.
4. "What's here" table row added by hand in both `with-git/README.md` and
   `without-git/README.md` (not generated by `render_templates.py`).
5. `python3 render_templates.py` then `--check` — paste output below.
6. Independent ROAST (separate reviewer session) → `roast.md`, PASS required before PR.

## 2026-09-20 — implementation, all in `/home/harness/harness-projects/1/ahr-sem04-wt56`

All three edits + both READMEs applied:

- `templates/base-project-template/common/.claude/memory-notes.md` — new, 5 bullets, placed in
  `common/.claude/` (not `environment/`). Opens with the CrewAI/LongMemEval figure (46.0% memory
  enabled vs. 57.6% disabled, same benchmark, CrewAI issue #5800) as the justification for why
  this is discipline, not a feature pitch.
- `templates/base-project-template/fragments/claude-core-bottom.md` — one added clause on the
  existing "Persistent memory" bullet, pointing at the new file.
- `deep-dives/components/instructions-rules/base-project-template/design-and-usage.md` — exactly
  one new growth-ladder row (`Memory / repeated context`), trigger wording matches the Work order 6
  spec's own ("Stage 2: owner repeating same information across sessions / Claude hitting same
  rake twice") verbatim in spirit.
- `templates/base-project-template/with-git/README.md` and `without-git/README.md` — one new
  "What's here" row each, hand-edited (matches existing row style/tone).

Ran, both from `/home/harness/harness-projects/1/ahr-sem04-wt56/templates/base-project-template`:

```
$ python3 render_templates.py
wrote with-git/CLAUDE.md
wrote without-git/CLAUDE.md
... (24 files total, incl. "wrote with-git/.claude/memory-notes.md" / "wrote without-git/.claude/memory-notes.md")

$ python3 render_templates.py --check
render_templates.py --check: PASS — both variants match their source fragments/common files.
```

`git diff --stat` confirms exactly the intended file set: 3 hand-edited source files (deep-dive,
fragment, +2 READMEs) plus the two auto-rendered `CLAUDE.md`s and three new `memory-notes.md`
copies (common + both variants). No other files touched.

Not overlapping `LESSONS.md`/`DECISIONS.md`: `memory-notes.md`'s own opening paragraph states
explicitly it is not a substitute for either, and its 5 points are all about auto-memory
*hygiene* (entry granularity, index-vs-log, derivability, trust boundary, growth trigger) — none
of them duplicate "log why a decision was made" (`DECISIONS.md`) or "the promoted permanent
subset" (`LESSONS.md`).
