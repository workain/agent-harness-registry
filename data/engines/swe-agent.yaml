name: SWE-agent
slug: swe-agent
homepage: https://github.com/SWE-agent/SWE-agent
interface: "CLI / library (agent scaffold you embed, not a standalone product)"
what_it_is: >
  An agent scaffold from Princeton/Stanford — takes a GitHub issue and tries to fix it
  autonomously using an LM of choice, via a custom agent-computer interface governed by
  a single YAML config. Distinct from but paired with SWE-bench (the evaluation
  benchmark, see `data/benchmarks/swe-bench.yaml`) — same ecosystem also includes
  Mini-SWE-Agent, SWE-ReX, SWE-smith, and sb-cli.
license: MIT
open_source: true
sandboxing: "Docker/Podman container backends via SWE-ReX, a remote execution framework that maintains terminal sessions on local machines or containers — all commands execute inside a container, host filesystem never directly exposed"
activity:
  stars: "20.0k (per gh api, 2026-08-10)"
  last_verified_note: "re-fetched live 2026-08-10 via gh api: resolves the prior fetch's stale-release flag — v1.1.0 (2025-05-22) is still the latest tagged release, but the repo itself was pushed as recently as 2026-08-03, so this is untagged ongoing development rather than a dormant project (contrast with this registry's aider entry, which is genuinely dormant on both fronts). 19.7k stars at the prior 2026-07-05 fetch"
provenance:
  - url: https://github.com/SWE-agent/SWE-agent
    note: "fetched live 2026-07-05 (org moved from princeton-nlp/SWE-agent to SWE-agent/SWE-agent)"
  - url: https://github.com/SWE-agent/SWE-agent
    fetched: "2026-08-10"
    note: "re-verified via gh api: star count refreshed (19.7k -> 20.0k), confirmed still-active push history despite the stale v1.1.0 release tag"
unverified:
  - "contributor count not extracted from fetched content"
deep_dive: deep-dives/engines/swe-agent.md
