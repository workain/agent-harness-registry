# vercel-labs/agent-browser

**Registry entry:** `data/components/skill-vercel-agent-browser.yaml` · **Category:** skills-tools

## What it is

Vercel's own headless browser-automation CLI for AI agents — reportedly accepted into Anthropic's official marketplace per secondary sources [unverified — marketplace-acceptance claim not independently confirmed].

## When to use it

You want a CLI-first browser-automation tool from Vercel specifically, rather than an embeddable Python library.

## How to get started

Install per the repo's CLI setup instructions.

## How it compares

Distinct from `browser-use` (this registry's companion entry): this is Vercel's own headless CLI, browser-use is an independent OSS Python library — pick based on CLI vs. library integration needs.

## References

- https://github.com/vercel-labs/agent-browser — verified via `gh api`/direct fetch where possible, 2026-07-05
