# mylee04/claude-code-subagents

**Registry entry:** `data/components/subagent-mylee04-assembler.yaml` · **Category:** subagents

## What it is

Smallest by stars in this batch, but structurally distinct: ships an "agent-assembler" that DYNAMICALLY GENERATES a personalized subagent team from your detected tech stack, rather than shipping static files to copy.

## When to use it

You want a subagent roster tailored to your specific stack automatically, rather than manually picking from a static catalog.

## How to get started

Run the agent-assembler against your project; it detects your stack and generates matching subagent definitions.

## Gotchas

- Lowest-adopted entry in this batch (35 stars) — the generative-assembly idea is genuinely distinct, but treat it as an experiment, not a proven-at-scale approach.

## How it compares

The only collection in this registry doing generative assembly instead of static curation — worth a look even at low stars for the distinct mechanism.

## References

- https://github.com/mylee04/claude-code-subagents — verified via `gh api`/direct fetch, 2026-07-05
