# wshobson/commands

**Registry entry:** `data/components/subagent-wshobson-commands.yaml` · **Category:** subagents

## What it is

A companion repo to `wshobson/agents` (this registry's existing entry): production-ready SLASH COMMANDS, not subagents themselves — a delegation-adjacent mechanism (a command often triggers a subagent) rather than a subagent definition.

## When to use it

You're already using `wshobson/agents` and want the matching command layer, or want reusable slash commands independent of any specific subagent roster.

## How to get started

Install commands per the repo's docs alongside or independent of `wshobson/agents`.

## Gotchas

- The weakest fit for a 'subagents' category strictly speaking — commands are delegation-adjacent, not subagent definitions themselves. Included for completeness since it's the direct companion to an existing registry entry.

## How it compares

Companion to this registry's `wshobson-agents` entry — install together if adopting that roster.

## References

- https://github.com/wshobson/commands — verified via `gh api`/direct fetch, 2026-07-05
