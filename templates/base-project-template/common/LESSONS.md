<!-- TEMPLATE FILE — delete this comment once you've read it. -->

# Lessons

The deliberately-**promoted** subset of things learned running this project — not the fast,
continuous layer (that's your runtime's own auto-memory, if it has one; see `CLAUDE.md`'s
pointers section). This file is small on purpose: it exists for the handful of lessons worth
one line of permanent, team-visible record.

**Promotion cadence (fill this in and actually do it — this line is the one that matters):**
`<e.g. "review auto-memory during each weekly retro / each release; promote anything confirmed
twice or that would have prevented a real incident; remove anything superseded">`. A lessons
file with no active promotion step reliably freezes after bootstrap — don't let this be a
write-once file that a runtime memory feature quietly does the real job of instead.

## Format

One entry per lesson:

```
- **<rule, stated plainly>**
  Why: <the incident or reasoning that grounds it>
  Applies when: <the condition that makes this relevant — helps judge edge cases later>
```

<Delete this template scaffolding once you have real entries.>
