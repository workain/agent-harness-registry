<!--
TEMPLATE FILE — this is a worked example of the module format, not a real module. Delete it
once you've added your project's first real one (or keep it as a style reference, renamed).
-->

## Example: two tools with the same apparent job

<Name the two things that look interchangeable but aren't, state which one is actually correct
here and why, and name the failure mode if the wrong one gets picked (what error/behavior an
agent would see, so a future agent recognizes the symptom instead of retrying the same mistake).>
