{
  "name": "signup-landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Landing page with a demo-lesson signup form. One static page, no backend.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^7.1.14"
  }
}
