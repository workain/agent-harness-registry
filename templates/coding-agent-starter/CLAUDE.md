# CLAUDE.md

<ОДНА СТРОКА: что это за проект и что для него значит «готово».>
Не пересказ кода — код агент прочитает сам. Эта строка ориентирует.

## Гейт

Одно правило, которое агенту нельзя нарушать самому. Начните ровно с одного — того,
цена нарушения которого для вас реальна. Примеры формулировок, выберите своё:

- Никогда не запускать деплой на прод без явного запроса.
- Никогда не трогать `payments/`.
- Никогда не мержить свой собственный PR.

Это просьба, а не закон: текст агент читает и может учесть, но не обязан. Механически
принуждает не текст, а проверка, которая выполняется, — в этом шаблоне такая одна, и она
названа ниже.

## Где что лежит

Указатели, не содержание. Каждая строка — один адрес и одна фраза «что там».

- «Готово» для этого проекта: `spec.md`.
- Почему принято решение: `DECISIONS.md`; с последствиями надолго — `doc/adr/`.
- Текущая задача, что уже проверено: `.tasks/active/`.
- Хук, который отклоняет коммит в `main`, и доказательство, что он срабатывает:
  `.claude/settings.json`, `.claude/hooks/selftest-branch-guard.sh`.

<!--
ЭТИМ ФАЙЛ И ОГРАНИЧИВАЕТСЯ НА ДЕНЬ 0 — И ЭТО НЕ НЕДОДЕЛКА.

Всё, что здесь написано, грузится каждый ход и облагает налогом каждый запрос.
Правило, написанное впрок, нечем проверить — оно тихо разлагается.

Разделы «Build, test, verify», «Repository etiquette», указатель на правило с шаблоном
путей и первая запись ADR заведены заготовками в `doc/deferred.md` и вносятся, когда
появится повод.

УДАЛЯЯ `doc/adr/` ИЛИ `.claude/`, УДАЛИТЕ И УКАЗАТЕЛЬ на них выше — но ровно указатель,
не больше. У `.claude/` это своя строка целиком. У `doc/adr/` это только вторая половина
строки про решения: остаётся «- Почему принято решение: `DECISIONS.md`.» — сам `DECISIONS.md`
никуда не уходит, он день 0.

Указатель на то, чего нет, — строка контекста в каждой сессии ни за что; ровно поэтому в
этом шаблоне нет ни указателя на правило с glob'ом, ни самого правила.

Удалите этот комментарий, когда он перестанет быть полезен.
-->
