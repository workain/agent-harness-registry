# CLAUDE.md

<ОДНА СТРОКА: что это за проект и что для него значит «готово».>
Не пересказ кода — код агент прочитает сам. Эта строка ориентирует.
Развёрнуто «готово» описано в `spec.md`.

## Ворота

Одно правило, которое агенту нельзя нарушать самому. Начните ровно с одного — того,
цена нарушения которого для вас реальна. Примеры формулировок, выберите своё:

- Никогда не запускать деплой на прод без явного запроса.
- Никогда не трогать `payments/`.
- Никогда не мержить свой собственный PR.

<!--
ДАЛЬШЕ ПУСТО — И ЭТО НЕ НЕДОДЕЛКА.

Всё, что написано в этом файле, грузится каждый ход и облагает налогом каждый запрос.
Правило, написанное впрок, нечем проверить и некому опровергнуть — оно тихо разлагается.

Разделы ниже появляются тогда, когда для них будет ПОВОД, а не на всякий случай.
Порядок, в котором они обычно и заводятся:

## Build, test, verify      — как только команды перестали быть очевидными
## Repository etiquette     — как только в репозитории больше одного человека/сессии
## Где что лежит            — как только этот файл начал толстеть (см. README, «правило трёх адресов»)

Шаблоны этих разделов — в `doc/claude-md-sections.md`. Скопируйте оттуда нужный,
когда повод появится. Удалите этот комментарий, когда он перестанет быть полезен.
-->
